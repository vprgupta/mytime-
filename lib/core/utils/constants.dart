class AppConstants {
  static const Duration focusSessionDuration = Duration(minutes: 40);
  static const Duration breakSessionDuration = Duration(minutes: 10);
  static const Duration notificationAdvanceTime = Duration(minutes: 5);
}